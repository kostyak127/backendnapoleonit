version: '3'
services:

  server:
    build: .
    container_name: backendnapoleonit-app
    ports:
    - "8000:8000"
    command: sh -c "cd /backendnapoleonit
             && alembic upgrade head
             && python main.py"
    env_file:
      - .env
    depends_on:
      - database
    restart: always

  database:
    image: postgres
    container_name: backendnapoleonit-db
    env_file:
      - .env